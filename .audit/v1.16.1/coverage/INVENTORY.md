# v1.16.1 — full doc-vs-impl coverage campaign (LABEL X loop)

**Goal (user-directed):** page-by-page, feature-by-feature, compare every documented function / class /
method / attribute in Kirito's builtins + standard library against the real implementation; verify the
impl is correct; add anything missing from the docs; and write comprehensive, dedicated `.ki` tests —
normal use + self-determined **edge cases** + **hardening** (does bad input raise the *correct* error
message? does a case that *should* error actually error, or is it a **silent error**?). Cover BOTH
functioning and error paths. Do it thoroughly even where prior tests exist.

**Constraint:** do NOT modify `src/` (no C++) — only `.ki` + `.expected` — UNLESS a real bug is found,
which is then a separate, gated fix. Each new test file: `tests/scripts/cov_<area>.ki` (+ `.expected`
generated by the actual `build-release/ki` / `build-debug/ki`). Validate with
`tools/scripts/bytecode_stability.sh` (must stay 0-diverged) after each batch.

**LABEL X** = "recompute what's still uncovered from this table and dispatch the next batch." Loop back
here until every row is `done` and the doc-vs-impl deltas are all resolved.

## Phase 1 (codebase audit, read-only → findings only) — DONE (see phase1_kwargs_inspect_traceback.md)
- [x] kwargs + inspect — **0 real gaps** (all fixed-arity natives support kwargs+inspect); 2 cosmetic
      doc notes (seek `whence` shown required though optional; Match.group `{"index"}` under-describes variadic).
- [~] throw/traceback — **3 realistic bugs**: a native `RegexError`(match-time)/`TensorError` escapes as a
      span-less `std::exception` → surfaces as `error: <what>` with NO line:col/traceback. Sites: regex
      match/allMatches (stdlib_regex.hpp:280,158), tensor `_setstate_` (stdlib_tensor.hpp:2096,2100),
      matrix/complex.vector >64M (stdlib_matrix.hpp:389, stdlib_complex.hpp:663). Plus serialize/dump
      open-file message quality. **FIX PENDING** (src change → gated) — deferred until the coverage
      agent wave finishes (don't rebuild the binary mid-agent-run).

## Phase 2 — per-area coverage (status: todo / in-progress / done)
Scope from the reference pages: 08-builtins (99 sigs), 09-types (162), 10-stdlib (696) — ~40 modules.

| Area | Doc section | Test file | Status | Findings? |
|------|-------------|-----------|--------|-----------|
| builtins | 08-builtins | cov_builtins.ki | **done** (307 asserts) | none |
| types: None/Bool/Integer/Float + String/Bytes | 09-types | cov_scalars_strbytes.ki | **done** (264 asserts) | surplus-arg SILENT accept on upper/lower/isX/hex/strip (arity gap); doc: round ties away-from-zero, String(Bytes)→repr |
| types: List/Set/Dict + dunders | 09-types | cov_containers_dunders.ki | wave2 (running) | |
| math+statistics+random+bisect+heapq | 10-stdlib | cov_math_stats.ki | **done** (325 asserts) | none |
| int (BigInt) + complex | 10-stdlib | cov_int_complex.ki | **done** (302 asserts) | none (bugs); doc: BigInt==Float int64-range caveat, .compare-vs-zero note |
| string mod + textwrap + regex | 10-stdlib | cov_string_regex.ki | **done** (216 asserts) | textwrap width≤0 accepted silently (triage: doc note vs validate ≥1) |
| json + csv + base64 + xml | 10-stdlib | cov_serde_text.ki | **done** (169 asserts) | base64.decode trailing-garbage SILENT ERROR (src fix); json cycle msg doc-delta |
| collections + itertools + functools + tee + enum + copy | 10-stdlib | cov_collections_fp.ki | **done** (188 asserts) | none |
| io + path + sys + time | 10-stdlib | cov_system.ki | todo | |
| hash + crypto + gzip + zlib | 10-stdlib | cov_hash_compress.ki | todo | |
| dump + serialize + semver + arg | 10-stdlib | cov_serde_misc.ki | todo | |
| tabular | 10-stdlib | cov_tabular.ki | todo | |
| matrix + tensor | 10-stdlib | cov_matrix_tensor.ki | todo | |
| net + parallel | 10-stdlib | cov_net_parallel.ki | todo | |

## Doc-vs-impl deltas + bugs found (consolidated; fix after the agent wave, gated where src)
SRC fixes (batch, gated):
- **base64.decode** silently accepts trailing garbage after `=` padding (`decode("TWE=X")`→[77,97]). Validate.
- **textwrap.wrap/fill** accept width≤0 silently (CPython raises). Consider validating width≥1 (or doc note).
- **Phase-1 throw/traceback** (3): wrap match-time RegexError + TensorError + matrix/complex.vector at the
  native boundary so they surface as KiritoError with file:line:col+traceback.
DOC-only fixes (no gate):
- **json** cycle error message: docs say `cannot serialize '<Type>' to JSON`; impl says `cannot serialize
  a cyclic structure to JSON` (cycle case). Add/clarify in 10-stdlib.
- **io.seek** `whence` shown required though optional; **regex Match.group** `{"index"}` under-describes
  its variadic form. Minor inspect/doc polish.
