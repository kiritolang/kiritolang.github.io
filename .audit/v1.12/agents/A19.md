# A19 — parallel / dispatcher

**Files in scope:** stdlib_parallel.hpp, dispatcher.hpp
**Status:** running
**Probe binaries:** `release/ki-linux-x64` (fast), `dist/debug-asan` (memory/UB), `dist/debug-tsan` (races).

## Findings

### A19-1  Spawning an entry-script function re-runs the entry file's unguarded top level → fork bomb
- **Severity:** Medium (High if a user routinely spawns from `<main>` without the `argmain` guard — catastrophic host DoS)
- **Kind:** resource-exhaustion / missing-guard
- **Location:** src/kirito/dispatcher.hpp:665-711 (`runWorker` → `worker.evalIn(src, modScope, sourceFile)`); guard at stdlib_parallel.hpp:529 only rejects `sf.empty() || sf == "<main>"`; main.cpp:227 runs the entry file with `chunkName = file` (the real path, NOT `<main>`).
- **What:** A worker rebuilds a spawned function's closure scope by RE-READING and RE-EVALUATING its whole source file. For an imported module this only re-runs harmless top-level definitions. But the ENTRY script is run (main.cpp) with chunkName = its real path, so its top-level functions carry a non-`<main>` sourceFile and pass the spawn guard. When such a function is spawned, each worker re-executes the entry file's **entire unguarded top level** — including the `spawn(...)` calls themselves — so every worker spawns more workers: an unbounded, exponential fork bomb of OS threads + worker VMs. The `<main>` check is dead for the CLI (the entry chunk is never named `<main>`).
- **Repro:** `p5b_inline.ki`:
  ```
  var io = import("io"); var p = import("parallel")
  io.print("top-level runs")
  var inline = Function(x): return x + 100
  var t = p.spawn(inline, 5)      # NO `if argmain:` guard
  io.print(t.join())
  ```
  → prints "top-level runs" forever, spawns threads until the box is killed (observed exit 137/143, machine saturated). Guarding the driver with `if argmain:` (p5c_guarded.ki) fixes it and is the documented pattern (sqldb/webserver examples), but there is **no diagnostic** when it is omitted.
- **Confidence:** confirmed (reproduced under release; had to `timeout`/kill).
- **Fix sketch:** Python's spawn detects this ("An attempt has been made to start a new process before the current process has finished its bootstrapping phase … you probably forgot `if __name__=='__main__'`") and raises. Kirito should do the same: set a per-VM "loading module body" flag around `worker.evalIn(src, …)` in `runWorker` (and around the entry-file `runSource`), and have `parallel.spawn` throw a clear, catchable error if called while that flag is set (i.e. from module/entry top-level during bootstrap rather than from inside the spawned function). Cheaper partial fix: name the CLI entry chunk `<main>` so entry-script functions are simply non-spawnable — but that breaks the legitimate `if argmain:` pattern the examples rely on, so the bootstrap-flag guard is preferred.

### A19-2  Dispatcher retains every Task forever — `tasks_` is never erased (confirms & extends Wave-1 A08-2)
- **Severity:** Medium
- **Kind:** resource-exhaustion
- **Location:** src/kirito/dispatcher.hpp:724 (`tasks_` map), 549-582 (`spawnTask`/`taskDone`/`joinTask`), 586-600 (`shutdown`).
- **What:** `spawnTask` inserts a `std::unique_ptr<Task>` into `tasks_` keyed by id and NOTHING ever removes it. `taskDone` and `joinTask` look up but never erase; only `~KiritoDispatcher`/`shutdown` frees the whole map. Every `spawn` therefore leaks a `Task` (its `std::thread` object + the full result **dump blob** `std::string`) for the entire lifetime of the dispatcher. A long-lived server that spawns one task per request (exactly the sqldb/webserver connection-worker model) grows without bound — the retained result blobs can be arbitrarily large. Sanitizers don't flag it (reachable until shutdown).
- **Repro:** spawn N tasks that each return a large value and join them; RSS grows ~linearly in N and never recovers after join (p1_manyspawn.ki scaled up; retention visible by code inspection — `tasks_.erase` appears nowhere).
- **Confidence:** confirmed (static; `grep tasks_.erase` = 0 hits).
- **Fix sketch:** After a successful `joinTask` (thread joined, result copied out), erase the entry from `tasks_` under `registryMutex_` — the Task is finished and its result already returned to the caller, so nothing else references it (`reap`'s non-claimer spins on `done` before the claimer erases; sequence the erase after both `reap` returns). A joined-but-not-yet-erased Task double-join is already prevented by the `joining` atomic. Alternatively cap/recycle ids and drop finished Tasks lazily. Note: the retention currently *masks* a would-be UAF (see Probe log) — the fix must keep `reap`'s pointer valid, so erase only the claimer, after it has finished `reap` and copied `result`.

### A19-3  `Barrier.abort()` is irreversible — `reset()` can't revive it, and the error text is wrong
- **Severity:** Low
- **Kind:** correctness-bug / doc (misleading diagnostic)
- **Location:** src/kirito/dispatcher.hpp:297-300 (`Barrier::abort` sets `aborted_`), 293-296 (`resetBarrier` does NOT clear `aborted_`); stdlib_parallel.hpp:431-435 (user-facing `Barrier.abort()`), 62-73 `throwWait` maps `Aborted` → "operation aborted (dispatcher shut down)".
- **What:** The user-facing `Barrier.abort()` reuses the internal shutdown-only `Waitable::abort()`, which sets the permanent `aborted_` flag. `Barrier.reset()` (`resetBarrier`) only breaks the current generation (`brokenGen_`/`generation_`/`count_`) and never clears `aborted_`, so once a program calls `bar.abort()` the barrier is dead forever — `wait()` returns `Aborted` even after `reset()`. Python's `threading.Barrier`: `abort()` → broken, `reset()` → revives. Kirito diverges. Worse, the surfaced error is `parallel: operation aborted (dispatcher shut down)` even though the dispatcher is perfectly alive and the user aborted the barrier — a confusing diagnostic.
- **Repro:** `p13_barrier_reuse.ki`: `bar = p.Barrier(1); bar.wait(); bar.abort(); bar.reset(); bar.wait()` → throws "operation aborted (dispatcher shut down)".
- **Confidence:** confirmed (reproduced).
- **Fix sketch:** Give `Barrier` its own user-abort that maps to the broken state (like `resetBarrier` but leaving the barrier permanently broken until `reset()`), OR have `resetBarrier()` clear `aborted_` too. Distinguish user-abort from dispatcher-shutdown-abort so `throwWait` can emit "barrier was aborted" rather than "dispatcher shut down". Keep the shutdown `abort()` (Waitable interface) irreversible for teardown.

### A19-4  `sys.exit()` with live workers skips the dispatcher destructor — workers abandoned, not aborted/joined
- **Severity:** Low
- **Kind:** resource / robustness (cross-cutting with A10 sys)
- **Location:** src/kirito/stdlib_sys.hpp:150-159 (`exit` → `std::exit`), main.cpp:179 (`KiritoDispatcher dispatcher` is a `main()` local).
- **What:** `sys.exit` calls `std::exit()`, which runs only static/atexit destructors — NOT `main()`'s local `~KiritoDispatcher()`, whose `shutdown()` is the sole path that aborts + joins worker threads. So `sys.exit()` while workers run abandons them: no abort, no join. `std::exit` then tears down static-duration state and runs atexit while other threads are still executing (a worker mid-write to a std stream / open file can produce truncated or interleaved output). No crash was observed (the design keeps no shared mutable static state for workers to race with during teardown, so the usual std::exit-with-running-threads UB is largely defused), but abandoning live worker VMs is not orderly shutdown.
- **Repro:** `p15_exit.ki` spawns 8 long CPU workers then `sys.exit(0)` — process exits immediately, workers dropped (no hang/crash under TSAN, but no orderly teardown either).
- **Confidence:** confirmed (behaviour), plausible (teardown-race hazard).
- **Fix sketch:** Have `sys.exit` request the dispatcher's `shutdown()` before `std::exit` when a dispatcher is present (or route exit through a normal return so the local dtor runs), so workers are aborted+joined deterministically.

## Test gaps (C++ and .ki)
- **No `.ki` regression asserting the `if argmain:` bootstrap requirement / the A19-1 fork-bomb guard** — once A19-1 is fixed, add a test that spawning an entry-script function from unguarded top level throws a clear error (and does NOT fork-bomb). Currently the entire golden `.ki` suite (`*_parallel.ki`) is careful to define workers in imported modules, so the footgun is untested.
- **No test that `tasks_` is reclaimed after `join`** (A19-2): add a C++ test spawning+joining N tasks and asserting the dispatcher's task count returns to ~0 (needs a test accessor) or RSS stays flat.
- **`Barrier.abort()` + `reset()` revival** (A19-3) untested — `test_parallel_sync.cpp` / `*_barrier` scripts exercise timeout-break and normal rendezvous but never the user `abort()` then `reset()` path.
- **`putnowait` / `getnowait` error paths** (full/empty) have thin `.ki` coverage; the timeout-race combinations (bounded queue, concurrent put+get with per-call timeouts) are exercised by my probes but not committed as regressions.
- **`Semaphore` initialized to 0 then over-released** (latch pattern) — no `.ki` test; C++ `test_parallel_edge.cpp` may cover — verify.
- **`net.socket.detach()` / `net.fromfd(fd)`** cross-VM handoff (A19 scope) — I did not probe (no TLS-free loopback harness handy in the scratchpad); flag as a coverage gap to confirm against `r4_net_parallel.ki`/`r9_net_parallel.ki`.

## DRY / single-source-of-truth notes
- **`_getstate_`/`_setstate_` cross-VM-identity boilerplate is copy-pasted 5×** — QueueVal/LockVal/EventVal/SemaphoreVal/BarrierVal each hand-roll an identical `_getstate_` (return `id()` as Int) and `_setstate_` (require 1 arg, `requireDispatcher`, `xById(...)`, null-check, throw "cannot rebind"). Only the registry accessor + type name differ. A single templated helper on `NativeClass` (given a `d.xById` lookup) would collapse ~90 lines to one. Same for the `bind`/accessor-lambda preamble in every `getAttr`.
- **The five `create*` / `*ById` dispatcher pairs** (dispatcher.hpp:538-547) are already funneled through `makeWaitable`/`byId` — good, that part is DRY. The Kirito-side registration in `ParallelModule::setup` (`registerDeserializer` ×5 + `m.fn` constructors) is the mirror of the C++ side; kept in sync manually.
- **`argTruthy` / `argTimeout` / `throwWait`** (stdlib_parallel.hpp) are the single source for arg coercion + wait-result mapping — reused across all primitives. Good.

## Probe log

## Probe log
- p1_manyspawn (200 spawns, join all): TSAN clean, correct sum 2646700.
- p2_queue (4 producers × 500 + 4 consumers, contended unbounded Queue): TSAN clean, correct 499000.
- p3_shutdown (8 workers blocked forever on empty `Queue.get`, main exits without joining): clean — dispatcher dtor `shutdown()` aborts all Waitables then joins; no hang, no TSAN report. Deadlock-safety invariant holds here.
- p4_nested (20 workers each spawn+join a leaf worker on the shared dispatcher): TSAN clean, correct 420. Nested spawn / shared-dispatcher access is mutex-safe.
- p5b_inline: **A19-1 fork bomb** (see finding).
- reap()/tasks_ retention: because `tasks_` is never erased, the `Task*` handed to a spinning non-claimer `reap` stays valid — the A19-2 retention *prevents* a UAF. Any fix to A19-2 must preserve that.
- p6/p7 barrier (broken-by-timeout, multi-generation rendezvous, 5 parties × 50 rounds): TSAN clean.
- p8 (8 workers × 2000 lock acquire/release), p9 (10 workers × 1000 on Semaphore(3)): TSAN clean, correct.
- p10 (bounded Queue(2), 3 pushers + 3 pullers × 300 with per-call 0.5s timeouts): TSAN clean, 900/900.
- p11 (`bar.abort()` from main while 3 workers block on Barrier(10)): workers unwind → [-99,-99,-99], TSAN clean. **Led to A19-3.**
- p12 (Event set-after-wait, 6 waiters): TSAN clean, 42.
- p13: **A19-3** (abort irreversible + wrong error text).
- p14 (3 fillers + 3 observers hammering qsize/empty/full/getnowait concurrently, 5000 iters): TSAN clean.
- p15: **A19-4** (`sys.exit` skips dispatcher dtor).
- p16 (20 workers blocked across ALL 5 primitive kinds — queue.get / lock.acquire / event.wait / sem.acquire / barrier.wait(100 parties) — main exits): dispatcher dtor `shutdown()` aborts every Waitable and joins all → clean, no hang, no TSAN race. **Core deadlock-safety invariant is SOLID.**
- Committed golden scripts under TSAN (probe_parallel_soak, probe_parallel_adversarial, r6_parallel, r8_parallel): rc=0, no race/warning output. Baseline green.
- Dispatcher synchronization review: `spawnTask` holds `registryMutex_` across the `tp->thread = std::thread(...)` assignment; every reader of `tp->thread` (`joinTask`/`shutdown` via `reap`) first acquires `registryMutex_`, so `tp->thread` writes happen-before any join. `done` is seq_cst store-after-`result`-write / load-before-`result`-read → the non-claimer `reap` spin path is correctly synchronized. No data race found in the dispatcher core.
- UAF-across-VM check: only dump blobs (`std::string`) cross the thread boundary (`packArgs`/`unpackArgs`/`dumpfmt`); no `Handle`/`Object` is shared between VMs. Confirmed by code + clean ASan on prior probes.

**Status:** DONE. Scope (stdlib_parallel.hpp + dispatcher.hpp) fully covered: spawn/Task, Queue, Lock/Event/Semaphore/Barrier, cpucount, cross-VM identity, shutdown/abort fan-out, nested spawn, timeout races. `net.detach()`/`net.fromfd` cross-VM handoff NOT probed (noted as a coverage gap).
